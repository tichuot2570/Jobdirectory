<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jobdirectory.jobdirectory">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".AndroidDatabaseManager"
            android:label="@string/app_name" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ListCategoryActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".JobDetailsActivity" />
        <activity
            android:name=".LoginAndroidActivity"
            android:label="@string/title_activity_login_android" />
        <activity android:name=".ListSpecializationActivity" />
        <activity android:name=".ListLocationActivity" />
        <activity android:name=".ListJobActivity" />
        <activity android:name=".JobActivity" />
        <activity android:name=".ListJobCompanyActivity" />
        <activity android:name=".EditJobActivity" />
        <activity android:name=".InsertDataDemo" />
        <activity android:name=".ListJobFavoriteActivity"></activity>
    </application>

</manifest>